name: Jekyll Build on Contentful Publish Event
on:
  repository_dispatch:
    types: [publish-event]
jobs:
  call-build-workflow:
    uses: ./.github/workflows/build.yml
    secrets:
      ACTIONS_DEPLOY_KEY: ${{ secrets.ACTIONS_DEPLOY_KEY }}
      TEKD_TEST_CONTENTFUL_SPACE_ID: ${{ secrets.TEK_TEST_CONTENTFUL_SPACE_ID }}
      TEKD_TEST_CONTENTFUL_ACCESS_TOKEN: ${{ secrets.TEK_TEST_CONTENTFUL_ACCESS_TOKEN }}